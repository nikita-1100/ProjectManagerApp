spring:
  datasource:
    driver-class-name: org.h2.Driver
    password: root
    url: jdbc:h2:mem:pm
    username: root
  jpa:
    hibernate:
      ddl-auto: create
    defer-datasource-initialization: true